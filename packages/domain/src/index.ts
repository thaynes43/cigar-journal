// @cj/domain — the Smoke aggregate and its bounded-context model (ADR-002).
// Intentionally empty: the domain model lands in the next slice. This file
// exists so consumers can import the package and vitest has a target.
export {};
