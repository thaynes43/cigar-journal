Disposable: the DESIGN-004 (ADR-012 Wave 4) before/after screenshot set, kept off the code branch because `lane-screenshots/` is gitignored by convention. Delete this branch once PR #NNN is reviewed.
